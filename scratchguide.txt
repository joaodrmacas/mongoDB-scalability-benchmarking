# Build the test images via
./build_containers.sh

# Run this to start the server
docker compose -f docker-compose.yml up --detach

# Then configure the server topology
./configure_3shards.sh

# Run the client you want with its config
# docker run -it --network host client_100w|client_100r|client_75r.25w node client.js 1|2|3 -r1|-r2
# Example
docker run -it --network host client_100w node client.js 3 -r1

# You can get the data after the client finished via
docker cp {client_container_name}:/app/results.csv {dest_folder}